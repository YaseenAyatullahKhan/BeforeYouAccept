# BeforeYouAccept
An AI-powered Terms and Conditions document checker ✨. Uses the Malaysian ETT, CPA, and PDPA in its RAG pipeline 🧐 to give you the red flags about T&amp;C Agreements before you mindlessly rush to click that accept button 👍

### Directory Structure
```
BeforeYouAccept/
├── .streamlit/
│   └── config.toml      # Theme settings (primary purple color)
├── assets/
│   ├── logo.png         # The logo
│   └── background.jpg   # The hero page image
└── src/
    ├── app.py               # MAIN CODE (The heart of the project)
    ├── utils.py             # Helper functions (API calls & PDF logic)
    ├── requirements.txt     # List: streamlit, jamai-base, fpdf
    └── .env
```
